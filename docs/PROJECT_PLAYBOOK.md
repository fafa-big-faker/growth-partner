# 项目开发与发布手册

音频低延迟约定（2026-09-11）：浏览器短音效含锻造循环统一使用 `AudioManager` 的 interactive AudioContext 与复用解码缓冲；Android 1.0.4+ 把固定白名单 WAV 预解码到内存并转换成设备最佳采样率，由前台持续预热、低延迟模式的单一 `AudioTrack` 混音，避免每个音效重新启动输出链路；不可用时自动回退同一 Web Audio 路径。BGM 继续网页单实例串流。切后台必须停止原生音轨和静音预热，回前台重启；静音、关闭奖励弹窗仍保留分组取消及异步取消保护。手机外放最终延迟需实机确认。回归入口：`android-app/tests/PcmAudioTest.java`、`tests/audio-web.test.js`、`tests/native-audio-bridge.test.js` 与 `tests/audio-web.browser-check.cjs`。

这份手册记录日常开发中最容易重复、遗漏或卡住的流程。项目总体说明仍以 `HANDOVER.md` 为准。

## 安卓入口与触摸交互

- `web-interactions.js/css`仅在登录、两种账号界面与游戏弹窗/浮层关闭原生图片保存菜单、文字误选和拖拽。输入框、textarea、select、contenteditable及`data-native-interaction`保留长按编辑与粘贴；动态节点使用事件委托，不反复绑定。不得阻止touchmove或关闭双指缩放。
- 游戏区域统一使用`-webkit-tap-highlight-color: transparent`去掉浏览器/WebView自带的蓝色点击闪框，由子元素继承；不能用删除outline或focus-visible的方式代替，游戏自己的按压动效和键盘焦点提示继续保留。
- `app-shell.js`向安卓容器提供`window.XianlaiShell.handleBack()`及`setBackgrounded(boolean)`，前者同步返回是否已处理，后者控制宿主前后台音频。Android另注入只含固定音效播放/停止的`XianlaiNativeAudio`与只含精确资源查询的`XianlaiBundledAssets`，禁止扩展为文件或网络接口。返回调用既有弹窗关闭按钮与Router流程，进行中的资源操作不得被系统返回中途跳走。
- `OperationGuard.isBusy()`只读当前是否存在任何资源操作，不修改锁或任务。`AudioManager.setSuspended()`暂停后台声音且不改用户静音偏好；回前台不补播旧音效或旧锻造循环。
- 安卓工程及构建说明在`android-app/`。固定应用ID`cn.xianlai.game`、名称“仙来”，主入口仍为现有GitHub Pages。图标初期复用已批准的修仙图标，新图提示词在`docs/art-prompts/仙来-应用图标.md`。
- 服务端存档由Supabase和原账号决定，打包不复制或迁移玩家数据。安卓WebView与浏览器的本地登录状态独立，首次使用需登录；重新打包覆盖安装须保持应用ID与签名、增加版本号。签名文件和密码只能保存在仓库外，禁止提交或在日志中输出。
- 网页发布后应用重新加载可获取新版，不添加强制中断正在进行操作的自动刷新。APK壳的功能改动需要新安装包；本轮没有离线游戏或自建增量热更新系统。
- APK 1.0.2起删除自建的大图标/“正在连接仙途”/圆圈启动占位，正常启动只使用与网页入境准备一致的纸面底色及WebView；`errorPanel`默认隐藏，仅连接/渲染错误可见。重试复用WebView时等新文档commit才显现，避免旧错误页闪回。不要再添加一套普通启动进度或品牌中转页；此类改动必须交付同签名新APK，不能仅推网页后声称旧包已更新。

## 1. 数据流

```text
飞书配置表 -> sync-config.py -> game-config.js -> GitHub main -> GitHub Pages
                                      |
                                      +-> 浏览器通过 Supabase 读写玩家数据
```

- 飞书是游戏数值的源头。
- `game-config.js` 是生成文件，禁止手改。
- Supabase 保存玩家状态、背包、签到、任务、邮件和提现等运行数据。
- 推送 `main` 后 GitHub Pages 自动更新正式网页。

## 天道任务与奖励配置

- 主题任务创建可选择进行中主题，名称与活动日期沿用玩家端同名归组结果；必须有一条已发布原任务正在活动期，不能把过去/未来主题的中间空档当成进行中。创建和读取仍防连点，失败保留表单。
- 任务创建和自主任务奖励审核共用AdminView奖励编辑器：通过`_renderAdminItemOptions`从ITEMS的规范ID去重并分组，玩家可见选项以名称/品质为主，不要求输入ID。`_addRewardRow`、`_removeRewardRow`及`_updateAdminItemPreview`管理行与现用图片，`_readRewardItems`严格读取正整数并合并重复道具。
- 砍树次数仍在独立字段设置，奖励道具列表不重复列type6；`_readRewardChopping`读取非负整数。创建/审核只在校验成功后调用原有DB逻辑，不直接改变玩家背包。
- 天道道具预览、背包、任务奖励继续共用`renderItemIcon/getItemIconPath`，不要在下拉选项或背包重新拼旧emoji作为图标。GM可以选择全部类型，与任务奖励选择器共享名称和图片来源。
- 发布池任务可通过`showEditTask`编辑，打开时strict读取最新数据并确认draft，复用创建表单及奖励选择器预填。保存只调用`updateDraftTask`条件更新id+draft，不改UUID、排序、发布状态或额外主题奖励；错误/零行保留输入，零行提示状态已变化。保存/发布等写操作共用逐任务锁，保存刷新保留当前筛选。
- `renderTaskManage`检查请求代次、账号、页面及可见性；旧请求不得在退出、切页、后续刷新之后回填。新建与编辑共用表单打开锁，同一时间只保留一个任务表单；编辑使用表单字段value赋值、列表文字转义，避免引号或尖括号破坏页面。
- 任务保存请求期间禁用所有表单输入和按钮，并临时加`modal-locked`阻止点背景或关闭；finally恢复原disabled状态（发布状态原本禁用的不能被放开），防止保存等待中后改的输入被成功关闭时悄悄丢弃。真实编辑预填、奖励数量、双击、失败重试与筛选保留由`tests/admin-reward-picker.browser-check.cjs`一并覆盖。

## 首屏、登录与游戏转场

- `scene-transition.js/css`只负责这两次入场：首屏720ms叠化到登录，账号和资源就绪后700ms叠化到游戏；背包/底栏子项晚80ms、6px归位，登录退出缩放1.02。复用现有美术，不生成新图片、全屏滤镜或额外声音。
- `LoginBoot`区分资源完成和`revealing`阶段，`whenReady`等转场结束才允许登录。`Auth.init`的onReveal与ready回退共用单次Logo初始化，传360ms延迟使书写在叠化中段开始，不能完成后再次init重播。普通装饰取消仍返回可用ready；销毁/过期实例才返回cancelled。
- `Auth.doLogin`先验证、加载数据、解码当前场景/人物，成功后才入场，登录锁持续到转场完成；有效验证后收起密码输入键盘。动画异常不影响已加载页面，真正渲染失败回到表单。准备目标页面的请求12秒未完成则回退，晚到渲染须检查session/代次；动画本身另外有180ms结束兜底。
- 只给退出的登录页临时fixed叠层，不对目标dashboard或底栏祖先做transform，避免改变固定定位与新手引导坐标。取消/后台结束须取消动画、移除临时样式并恢复inert。减少动态时160ms淡入淡出，无平移缩放。
- `startFirstChopGuide`在转场active时不启动；Auth在完成后重新核对当前账号/页面再调用，避免透明引导抢焦点或提前拦点击。`tests/scene-transition.browser-check.cjs`以真实文件/动画和模拟数据验证三视口、Logo连续性、输入锁、失败重试、取消、后台和减少动态，不截图或访问真实数据库。

## 2. 飞书配置约定

- 第 1 行：英文参数名，供程序稳定读取。
- 第 2 行：中文字段名，供策划阅读。
- 第 3 行起：实际数据。
- 新增列时，先确定英文参数名，再修改 `sync-config.py` 的必需字段和输出结构。
- 同一格中多个 ID 或数量用英文逗号分隔；并列的 ID 与数量必须一一对应。
- 奖励包当前字段：`pack_id`, `item_ids`, `item_quantities`, `quality_id`, `quality_note`。
- 每日签到当前字段：`item_ids`, `item_quantities`。
- 商店说明字段：`description`；`note` 仅供内部备注，不展示给玩家。
- BUFF 的 `type` 为必需整数：`1` 掉落倍率，`2` 返还砍树次数。按英文表头读取，不按列号、BUFF ID 或新文案推测；非法值要报告具体行号。

## 3. 同步配置

在 Windows `cmd` 中进入仓库后执行：

```bat
set "PYTHONUTF8=1"
set "LARK_CLI=%USERPROFILE%\.trae-cn\plugins\trae-remote-official\lark\1.0.4\bin\lark-cli.exe"
%USERPROFILE%\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe sync-config.py
```

成功标准：

- 所有工作表均打印读取条数。
- 最后出现“已生成 game-config.js”。
- 没有“缺少英文参数名”或权限错误。
- `game-config.js` 中可找到新字段及最新数据。

若失败，按顺序检查：

1. 飞书第 1 行英文参数名是否完整且没有多余空格。
2. `LARK_CLI` 是否指向存在的可执行文件。
3. 飞书 CLI 是否仍有读取权限。
4. 工作表名称是否被改动。

## 4. 本地测试

本机优先使用 Codex 自带 Node：

```bat
%USERPROFILE%\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe --test tests\*.test.js
```

JavaScript 语法检查：

```bat
%USERPROFILE%\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe --check app.js
%USERPROFILE%\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe --check game-config.js
%USERPROFILE%\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe --check character-animator.js
%USERPROFILE%\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe --check ten-chop-timeline.js
```

Python 语法检查：

```bat
%USERPROFILE%\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m py_compile sync-config.py scripts\split_axe_sheets.py scripts\split_idle_axe_sheets.py
```

最后执行 `git diff --check`，并检查 `git status --short` 是否只有本次需求文件。

## 5. 功能实现底线

- 合成、签到、领取、购买、锻造、出售、装备、砍树等资源操作必须防重复点击。
- 消耗和奖励尽量放进 Supabase 原子 RPC；暂时无法原子化时必须有失败补偿。
- 数据库返回失败时不能先显示成功，也不能只改本地界面。
- 重绘背包时保留当前页签，不把玩家强制带回道具页。
- 十连操作先一次性计算和保存，再播放表现时间轴，网络延迟不能改变动画节奏。

### 独立仙斧与随机词条

- 普通材料继续保存在 `inventory` 并按道具 ID 叠加；仙斧只保存在 `weapon_instances`，每把都有独立 UUID。
- `player_state.axe_id` 保留为当前外观道具 ID；真正的装备归属以 `axe_instance_id` 为准。
- 锻造时先根据道具的技能 ID，从同 `buff_id` 的配置中按 `weight` 抽档，再从 `value*_range` 生成数值。
- 抽中的 `buffRowId`、`buffQuality` 和最终数值永久写入 `skill_rolls`；砍树时只读取这些固定值，禁止再次随机强度。
- 新词条同时冻结配置 `type` 与兼容的 `effectType`，显式 `type` 优先。历史实例缺少 `type` 时保留原 `effectType`，更旧记录才兼容原描述；不为补类型迁移、重抽或修改玩家数值。
- 锻造、装备、出售必须分别调用 `forge_weapon_instance`、`equip_weapon_instance`、`sell_weapon_instance` 原子 RPC。
- 历史斧头迁移后若词条为空，由 `initialize_weapon_affixes` 只写一次；并发情况下以后端已保存结果为准。
- 技能文案只给动态参数添加 `buff-quality-*` 颜色，正文保持中性，避免整段高饱和影响可读性。

## 6. 发布

发布前清单：

- 配置同步完成。
- 全部测试通过。
- JavaScript 和 Python 语法检查通过。
- 图片脚本验证通过。
- `git diff --check` 通过。
- `git diff` 中没有密钥、临时文件和无关修改。
- 用户没有要求视觉验收时，不额外打开页面做视觉验收。

发布命令：

```bat
git add <本次文件>
git commit -m "描述本次玩家可感知的改动"
git push origin main
```

推送成功后，GitHub Pages 会自动部署。推送只是上传完成，须确认对应提交的 Pages 运行成功，并核对线上入口和本次修改文件的实际内容，才能报告“线上已发布”。

- 若产物上传收尾 `FinalizeArtifact` 返回 403 而构建成功，先查远端日志，不因服务端上传失败修改前端配置。
- 本项目旧的失败任务重跑曾卡在未入队状态，普通和强制取消均返回409。遇到同一情况不反复重试：使用仓库 `POST /pages/builds` 为当前分支触发一次新构建，跟踪新运行。
- 不用空提交触发重试，不删除部署记录；等待十分钟无进展时按项目规则汇报原因。

## 7. 回滚

已发布版本出问题时：

1. 找到有问题的提交。
2. 用 `git revert <commit>` 生成反向提交。
3. 重新跑测试。
4. 推送新的回滚提交到 `main`。

不要使用 `git reset --hard` 或强推覆盖共享历史。

## 8. 凭据安全

- 不在对话、代码、截图、提交或 remote URL 中展示 token。
- GitHub 使用本机凭据管理或 OAuth；remote 保持普通 HTTPS 地址。
- Supabase 浏览器端 publishable key 可以公开，但数据库必须通过 RLS/RPC 限制权限；service role key 绝不能进入前端。
- `apikey`、`.env`、证书和服务账号文件必须留在仓库外或被 `.gitignore` 排除。

## 9. 登录便利性与动作同步

- 登录密码由浏览器原生密码管理器保存；真实表单保留 `username` 和 `current-password` 标识，静态站点禁止表单外发。不能为了自动填充把密码写进网页存储。
- 修炼者与天道使用不同凭据标识；切换角色时清空旧密码，迟到的自动填充不能覆盖用户输入。仅完成验证和初始化后请求保存；首次浏览器可能要求确认保存，不支持的浏览器保留普通表单行为。
- 十连砍所有表现使用同一 `TenChopTimeline.getStep(i)`。按钮每一步调用 `_playChopButtonFeedback(button, timing.speed)`，不能在等待网络时加播一次，也不能另建独立间隔定时器。
- 动画使用可重入的清理机制：重播前取消旧清理定时器，防止下一段被上一段截断。
- 纯展示角色、特效和装饰层必须点击穿透；透明图片仍以整张矩形拦截指针，不能认为透明像素自动穿透。人物前景层不得阻挡仙树交互。
- 相对按钮定位的次数/状态角标必须放在按钮或专用按钮容器内，不能挂到还包含其他操作的整组容器。覆盖装饰角标不接收指针，邻近控制预留明确间距。
- 替换角色图片后，必须同步更新实际运行资源 URL 的缓存版本，不只更新 JS 文件版本。
- 美术提示词与动效方向先交付，临时示意不得直接冒充用户生成的正式资源。当前四组生成提示词在仓库同级 `仙来-美术生成提示词.md`。

## 10. V7 统一修仙与背包

- `mobile-cultivation.js/css`在电脑和手机均使用常展开3:2双栏。模块名保留兼容，`isEnabled()`判断共享布局是否可用，`isMobile()`只描述物理屏幕。左侧唯一`inventory-grid`恒为道具，右侧当前装备或两列武器库；切换屏宽不再恢复旧双tab布局，也不改写遗留`currentInvTab`。
- 整个双栏仅铺V7纸面，不在左右内容后各套一层框。左侧数量、新标记、触摸尺寸保持清晰，较窄手机减为三列，长技能在右侧内部滚动。
- 右下“武器库”进入右侧武器格后变“返回”；不换装也能返回。按UUID显示每把斧头，当前装备排首位并标“当前”；点武器只看详情，明确点装备才换装，已装备不可出售。
- 整页修仙刷新前`MobileCultivation.unmount({ preserve: true })`，刷新后`mount(snapshot)`；背包重绘前后调用`beforeInventoryRender(tab)`与`refreshInventory(tab)`，右栏通过`refreshEquipment({html,weaponsHtml})`更新，保留两栏滚动及库模式。
- `Router.playerTab`调用`setPage(tab)`；离开修仙保留背包快照，任务/商店中心变成返回修仙。侧边仍为任务与天道酬勤，非修仙页不显示十连、锻造、砍数。退出登录`unmount()`完全清理监听和移动DOM。
- 短屏人物与树通过同一场景容器整体缩放，不分别改变锚点。新布局不得破坏十连时间轴、点击区域或砍树次数相对按钮的位置。
- 同宽变高及全屏进出不应移动或重建背包DOM，不重置武器库模式及两栏滚动。重绘时继续跳过转换阶段的临时滚动夹值；跨宽度时浏览器可以把超出新滚动范围的位置夹到有效值，不强行恢复无效偏移。
- 手机右栏始终将斧头图与名称并排，48px图高度不再因全屏或高度断点改变；桌面显示88px图。技能首行保留可见空间。
- 武器格按列宽确定3:4高度，图片不得参与隐式行高计算。回归要给足多行武器，逐行断言底边不越过下一行顶边，不能只查数量或图片宽度。
- 数量与“新”使用小号深墨/朱红字，不用大底板遮图；纸面按钮有hover、active与focus。底栏操作区锁定124px（短竖屏112px），纸面固定72px（短竖屏64px）；safe-area只在其下方留空间，不加进纸的高度。全屏时整体随视口底部定位，不拉长九宫格填充。
- 背包固定手机224px、短竖屏180px、桌面280px；场景吸收多余屏高，不按剩余空间比例拉高武器栏。主内容不足时允许滚动，不能用overflow:hidden裁掉武器库按钮。桌面主区域最大620px，武器格宽不超过80px。
- 任务/商店返回保持与砍树同尺寸的圆形墨底，使用用户生成的`assets/runtime/ink-controls/return-arrow.webp`，60px容器内等比显示；仅替换箭头，不叠另一个圆底。退出时恢复原图、文字与状态，不改路由含义。
- `node tests/mobile-cultivation.browser-check.cjs`是无截图、无真实账号的事件/几何检查。除七种基础视口外，覆盖390宽680/950/1200高度往返及1440宽900/1200往返，实际调用requestFullscreen/exitFullscreen验证两栏、纸面和角色尺寸不变。依赖路径可由`PLAYWRIGHT_MODULE`和`BROWSER_EXECUTABLE`指定。
- 角色突破展示读取配置名称与要求，不再渲染仙阶emoji；保留icon配置兼容。修改名称只在飞书执行，再同步生成配置。
- 角色经验条使用`ink-controls/exp-track.webp`与`exp-fill.webp`。两层始终同宽同高、端帽保持固定；满槽用`--exp-progress`控制clip-path揭露，不设置填充图宽度来挤压纹理。初次渲染与轻量刷新共享`getExperiencePercent()`，保留真实经验与目标文字及ARIA，经验数字按目标位数预留稳定宽度。满级行为仍以当前配置/升级逻辑为准，不凭美术改动添加新的等级上限。

## 11. 登录动效与页面纸面样式

- 登录出场只播一次，结束后 Logo 以 6.4 秒周期轻浮 10px；不要让表单跟着移动。V5已撤掉程序水纹，改用用户墨纹图集，不再恢复旧水域遮罩或椭圆线条。
- 登录页由第14节`LoginBoot`完成图片与功能就绪后统一显现，随后连续可见500ms再播放1500ms Logo出场。`LoginArt`内部2500ms背景放行仅保留给独立兼容调用，正式入口不能靠它跳过关键图片。隐藏时取消出场等待，回来重新计时，减少动态模式直接静态显示。
- `login-art.js`围绕Logo排布真实墨纹：手机2条主墨迹+3段碎墨，电脑3+5；分条轻弯纹理，保留飞白。Logo按实际字形alpha及浮动范围保护，输入/加载区整个矩形加12px避让，装饰点击穿透。
- `LoginArt.getImageAssets()`返回六个版本化URL。正式入口由LoginBoot预备后注入已解码装饰，失败可简化进入；不加入点击登录后的游戏资源队列，也不能为等待装饰启动空动画循环。
- 登录动效需要响应页面隐藏、离屏、退出登录、窗口尺寸和减少动态偏好。手机限制绘制频率与画布分辨率，不在游戏内继续运行登录动画。
- 点击登录后在验证请求之前显示按钮内“正在入境”，表单不隐藏，角色和密码暂时锁定。公共资源统一在入境准备阶段完成；不再启动第二张加载面板或重新下载音频。按钮辅助名称随身份验证、账号读取、当前角色解码变化；失败恢复原表单并释放操作锁。
- 错误密码、初始化失败和路由失败都必须恢复表单、解除锁；异步回调要检查当前尝试仍然有效，不能把恢复的表单再次隐藏。
- `ink-pages.css`只覆盖任务、天道酬勤及提现记录的纸面布局；主题分组不再套大卡片，任务奖励直接可见，商品说明完整换行。人民币余额与小钱钱分开标识，提现记录只保留一个按需加载入口。
- 页面刷新要同时考虑页面自身与文档滚动容器，保留筛选项和当前位置；不因领取奖励重新排序或跳回页首。
- `ink-scrollbars.css`放在全部界面样式之后。只改原生滚动条外观，不改变背包格子、overflow、滚轮或触摸行为。Chromium标准滚动属性会覆盖WebKit伪元素，需保留兼容分支和高对比度回退。
- `node tests/ink-polish.browser-check.cjs`进行无截图、无真实账号的登录动效/加载状态/任务商店文字与事件检查。连同手机背包检查和全套单元测试执行，不替代用户明确要求时的视觉验收。
- 动效测试不能只判断Canvas存在非零像素。V5需要解码真实墨纹、检查持续变化与足够透明度，并断言输入区无墨影。插入CSS动画节点后先等绘制帧，再计算测试延迟；只有setTimeout不能保证无头浏览器已启动动画时间线。
- 短时CSS受击动效的可见性测试要等`Animation.ready`，再在实际delay与duration的有效区间取样；不能以墙钟等待300ms代替动画进度。游戏运行时仍按原时间轴播放，测试不得修改产品动画速度。
- 弹窗和手机背包关闭统一使用本地Lucide X（ISC许可随资源保存），44px点击区域、22px图标；不能再回退到红金圆章。保留辅助标签、焦点、按下态和锁定逻辑。
- 累签说明有独立浅底和深墨色，正文及重置时间对比度均须达到4.5:1；里程碑文字直接来自配置，不另写死3/7/14/28。
- V5任务与商品用`assets/runtime/v5/ui`真实纸框九宫格，切片来自manifest。标题#252b29、正文#464c49、辅助#626762；描述14px，辅助不低于12px。绿色仅用于状态，品质子节点保留原品质色。
- 任务/天道酬勤页主标题、任务分组标题与小钱钱兑换标题不加纯色色块，使用透明背景和深墨文字；纸面仅用于实际内容区，不给每行标题另套底板。
- 手机背包已由V7双栏替代旧抽屉；旧版V3入口条和V4抽屉纸面不再用于手机背包，详见第10节。

### 锻造结果与 V6 美术

- 锻造弹窗仅保留“天工开物”主标题。常规外框最大360px宽、520px高，短屏受100dvh减24px限制；各状态主锻造按钮保持原位。非滚动的外壳必须覆盖通用scrollbar-gutter:stable，避免右侧空轨道造成整体左偏。
- 场景使用`forge-workshop/stage.webp`，中央完整放置斧头，不为操作预留底部行。`forge-result-action`绝对定位在场景底部；装备按钮使用用户的`equip-slip.webp`，视觉96px宽、44px点击高，纸面深墨字。装备/仙阶要求/已装备共用锚点，不挤动轮播图；抽取中清除浮层操作。
- 名称下紧接独立品质/评级摘要；无技能在摘要同行显示小字“无斧技”，趣味文案紧随。删除160px空信息槽，空余空间由真实场景吸收；有技能时详情最多126px（短横屏60px）局部滚动，只有一个技能滚动层。抽取时替换为紧邻名称的状态/进度。
- 折叠概率入口在内容区底部，列表展开向上覆盖内容、不推移底部材料/再次锻造按钮。再次锻造清除旧摘要、技能、装备操作并重置内容滚动；初始/失败回到无问号场景与“以石为引，淬炼仙斧”。缺料提示读取配置的实际材料名，不写死锻铁。保留2至3秒、前60%推进进度/后40%高速轮播的原时间轴。
- 仙斧评级调用`WeaponAffixes.getWeaponRating(weapon)`：只读该实例保存的`skillRolls`，有效技能的`buffQuality`1至5对应B/A/S/SS/SSS，多技能取最高，无技能为B。不要用斧头品质、BUFF组ID或奖励目标品质代替。
- 正式字标用`renderWeaponRating(weapon, context)`统一接入：武器库格底、锻造结果独立摘要的品质旁、当前装备品质旁、仙斧情报品质旁。格底36x18px，行内48x24px，图片自身保留透明边和统一字高；不加徽章底板或放大格子。当前状态改右上Lucide对勾，锁与“新”在顶部状态区分行；手机品质/评级一行放到斧图和名称下，防止窄列挤压。仅图片失败时用真实评级文字同尺寸兜底，不改变占位。
- 换装/背包重绘时评级读取当前UUID实例，同道具ID的不同副本不能共用评级；保留两栏滚动和武器库模式。锻造成功显示评级，下一次抽取清除；保持当前紧凑摘要布局与既有锻造时间轴。`tests/weapon-rating.browser-check.cjs`检查320/360/390/1440视口、实际图片解码、五评级、混合角标、换装/滚动、结果切换和图片失败兜底，无截图/真实数据写入。
- V6品质使用用户生成的平面竖向墨记，替换左上细色条，不使用玉石或立体高光。12x26px容器内等比完整显示，桌面和挂在body的手机背包共享映射；不改物品格、数量和“新”标记，不拦截点击。
- V6登录按钮保留真实submit元素和“踏入仙途”辅助名称，墨刷/白色书法字独立位图。仅文字在悬浮或键盘聚焦时以180ms渐亮；按压scale(.98)，加载禁用，减少动态时不位移。不对整个按钮加亮或让表单跟着移动。
- 两层图片都成功后才替换文字兜底；正式登录入口先由LoginBoot完成两层图片预备，失败保留重试，不暴露缺图表单。独立LoginArt仍保留文字兜底兼容，销毁时清理load/error监听。
- 原美术提示词见`docs/art-prompts/仙来V6-品质徽记与登录按钮.md`；其中玉印方向已被最新品质墨记方案取代，实际切图和运行参数以`docs/ASSET_PIPELINE.md`第13节为准。

## 12. 砍树命中特效

- 使用一枚墨色斩痕和默认4片青灰落叶，不再用旧版金色爆闪、法阵或带旋风的整张叶片。
- `CultivationEffects.playHit({scene, tree, intensity, speed})`在原时间轴位置调用，十连传入`timing.speed`。CSS延迟`180 / speed`等待斧头下落，延迟必须计入清理时间；不要新增独立砍树计时器。
- 新五阶段宝树使用一致的归一化透明画布，受击点为图像内176/384、272/384；1x为384x384、2x为768x768，切换密度不能改变显示大小。树冠落叶锚点按各树原画测量，保存在外观描述及资产manifest。缺少元数据的历史目标才回退树框42%/45%。所有位置从实际显示坐标换回场景坐标，不能把缩放后的屏幕坐标当成本地像素。
- 特效最多24个活动节点和清理定时器，`clear()`与页面隐藏清理，减少动态时只留一枚短淡出。所有装饰点击穿透。
- 首屏预载与特效实际请求必须包含完全相同的版本化URL，否则浏览器会重复下载。新叶片位于`assets/runtime/effects/leaf-ink.webp`。

### 五阶段祈愿宝树（2026-09-09）

- 皮肤完全读取飞书`tree_appearance`，当前0至3阶为`tree_01`，4至6阶为`tree_02`，7至9阶为`tree_03`，10至12阶为`tree_04`，13至15阶为`tree_05`。前端不按灵阶写死映射；旧`sprout/spirit/divine`兼容第一/三/五棵，未知值回退第一棵。
- 五款统一主干受击点与根部y360，不能再按每棵树冠独立居中、缩放。默认画布216px、left=-103px、bottom=12px；窄屏180/-79.5/8，桌面288/-125/6。`observeTree`通过ResizeObserver按场景实际高度缩放整个角色与树，以最高树冠y47预留空间，五皮肤使用同一比例；不只缩树或挪人物。CSS短横屏0.78仅作初始化兜底。退出/换场景要断开尺寸观察并清除旧transform。树在角色后方，装饰图层点击穿透。
- 升级提示按每棵实际树冠顶部定位，名称绝对定位，不占用图像高度；树图本身不旋转或缩放呼吸，避免树根及落斧点漂移。
- 每棵只叠一张同画布的原画亮部，`screen`混合，8秒opacity从0.06至0.18再返回。不可添加模糊光圈、射线或全树增亮。`CultivationEffects.observeTree`跟踪可见树，离屏、隐藏及离开修仙停止；减少动态使用静态亮部。
- 原图导入和指纹见`ASSET_PIPELINE.md`第18节。DPR大于1选择768px的`@2x`树图/亮层，使用`wish-trees-hd-20260910`；普通密度保留384px原图与`wish-trees-20260909`缓存。预载、场景与详情共用所选密度的十个URL，不重复下载两套。高清图直接从原atlas导出，不能放大384px旧图、加锐化滤镜或修改锚点掩盖模糊。
- `tests/wish-trees.browser-check.cjs`用模拟数据检查五视口五皮肤、真实图像/透明边、统一锚点、树冠/主干点击、升级提示与动效清理，不截图或修改玩家数据。另用`--dpr=1/2/3/4`分别检查对应密度的实际解码与预载请求。

## 13. V7 品质奖励展示

- `reward-presentation.js/css`只负责表现，显式注入道具配置和图标函数，不接触数据库、随机权重或物品发放。
- 十连结果按真实`isExtra`分组，常规十次5+5，额外奖励单独居中；不能用数组第11项硬切。单次奖励和额外奖励复用墨团组件，结算只保留真实数量，倍率仅在对应格的技能演出期间提示；返还次数只在砍树按钮上方反馈。
- 使用V7五色墨团，不增加发光方框。纸上品质名称使用同色相较深文本色，不修改飞书配置颜色，不让金色在纸上看不清。
- 普通道具详情标题应用自身品质色，武器详情继续以技能为重点。图片路径、预载和缓存版本保持一致。
- 奖励弹窗须分别检查单抽和全BUFF十连。头尾不滚动、内容区可滚动，短屏中不滚动也能点击确认；不能只验整个弹窗在视口里或奖励排成5+5。
- 奖励footer按钮扩大点击区时必须同时水平/垂直居中文字；使用真实文本Range与按钮矩形的中心点检查，不能只确认按钮本身位于中间。

## 14. 背包整理、奖励演出与登录就绪

- `inventory-order.js`只对道具显示副本排序：数值类型ID优先、数值道具ID其次。按`DB.playerRole`隔离本地显式顺序，新种类追加、数量变化不重新排序；不改`Game.inventory`原数组、不清新标记、不排序武器。整理按钮固定左栏底部，44px点击高度；只让左滚动归零，保留右栏模式和滚动。
- `MobileCultivation.mount`通过`onSort`接收回调，不能假设顶层`const PlayerView`是`window.PlayerView`属性。锁定标记使用本地Lucide锁，14px，不恢复旧位图锁。
- `WeaponAffixes.applyRewardMultipliers`在原随机调用中记录`baseQuantity`和`buffTriggers`的前后数量、倍率及技能来源，不在表现阶段重新随机。额外奖励不套用普通掉落技能。
- `RewardPresentation.playReveal(overlay,{audio,onComplete})`只消费已到账结果；固定位置逐个揭示，只有类型1按真实记录演出倍率。每次触发固定1300ms：提示及音效300ms、旧数字晃动500ms、换新数字放大回落500ms；直到结束才播下一件，不因多次触发缩短。类型2不参与结果时间轴。`finish()`立即显示全部，`cancel()`清理；减少动态保留逐项和斧技信息，只取消运动，不再直接最终态。不能因为表现新增资源发放。
- 格内结算只留图标、名称和真实数量，禁止数量后再接一个容易误解的“×N”。类型1触发时，在该道具名称原位置临时显示“斧技·N倍！！”，颜色来自本次实际触发的BUFF品质。窄列可使用半角双叹号及紧凑文案，完整提示保留在辅助标签中，不能裁掉标点或撑高格子。
- 普通品质1/2无技能道具按210ms节奏完整出现，有有效类型1技能时先保留真实名称300ms。品质3出场880ms（240ms起图标），品质4/5出场1280ms（260ms起图标），图标分别320/340ms回弹；完整出场结束后再额外保留真实名称/基础数量300ms，才斧技或下一件。同一道具连续技能不重复停留。出场只读取道具品质，技能文字与数量色只读取实际BUFF品质。
- 珍稀出场使用用户12帧“墨破现宝”图集`assets/runtime/reward-bursts/rare.webp/high.webp?v=reward-burst-20260910`，最终静态背景仍用V7品质墨团。不得恢复上一版简单旋转缩放墨团及echo层。图集4×3、单格256、整体1024×768，真实img按400%/300%放进裁切容器，以steps(1,end)离散切格；不能用连续滑动串格，也不能逐帧按内容重新居中。
- 珍品容器160%、高档180%，只局部溢出art框，不增加格子/弹窗尺寸；道具置于特效之上，其他已展示结果不重新演出。神品玫紫/仙品暖金色调仅作用于高档图集，不改变道具/BUFF颜色。结束、跳过及减少动态隐藏整个爆发层，并取消其动画；减少动态的高档选择器要覆盖常规高特异性规则，不能只写低特异性animation:none。
- 图集单格裁切正确不代表整段墨爆完整：含特效的弹窗滚动区固定留26px顶部绘制空间，侧面以负margin/等额padding扩展26px到纸面边缘，最大弹窗尺寸不变。留白由资源是否存在决定，不随动画类开关，避免演出推挤底部按钮。滚入视野时按真实墨迹顶部计算（rare/high的帧内最小y为29/23）；检查单抽、十连首行左右角及末行的12帧alpha范围是否落在所有祖先裁切边界内。
- 加强版rewardRare/rewardHigh完整时长0.88/1.28秒，实测重音约394/429ms；图标回弹视觉峰约400/430ms，图集主爆发第6帧约380/410ms。新两音频单独使用`reward-burst-20260910`版本，原11声音不改；不叠普通rewardReveal，场景落物音不变。原图与原始加强WAV留在仓库外，导入及测量见资产手册第7/20节。
- 两张图集加入`getInitialGameImageAssets`和`RewardPresentation.getAssetUrls`，实际URL完全相同，首屏下载并纳入公共图解码，禁止到抽中时才临时加载。`tests/reward-bursts.browser-check.cjs`检查真实解码、12帧裁切坐标、峰值时刻、颜色、结束/跳过清理，不能只判断存在CSS动画名。
- 待展示项同步隐藏，等待两次requestAnimationFrame再启动，后续只安排下一组相对延迟事件，不能一次排满整条时间轴。页面重绘阻塞时，未来奖励必须继续逐项展示，不能追赶墙钟时间一起出完。取消要同时清除帧回调/定时器/本组声音；真实doChopTen入口、主线程阻塞与减少动态由`tests/reward-entry.browser-check.cjs`覆盖，不能只测预解码且等待弹窗入场完毕的独立fixture。
- 旧数量晃动期间保留原样，到800ms换新数字时才变成“×N！”并采用该BUFF品质色。演出结束后恢复道具名及道具自身品质色，数量则持续保留单叹号和最后一次实际生效技能的品质色，不取最高品质。连续技能、显示全部、隐藏、减少动态及静态结果都遵循此规则；普通道具、返还技能与额外奖励不加此标记。重新播放先清掉最终标记，回到普通基础数量。
- 已撤销底部公共技能条及其32px留白，不能通过隐藏文字而保留空框。局部提示与图标、数字共同进入视野，短屏只调整弹窗内容区的滚动，不滚整页。内容仍支持滚动且隐藏轨道。
- 单次与十连共用`_startRewardReveal`：单次传`single:true`，按钮始终“收下”且一次点击立即关闭；十连演出中“显示全部”，跳过或完成后“收下”。延迟单次弹窗留在砍树操作锁内；路由/退出通过`cancelChopPresentation`作废旧表现和计时器，不能离页后再弹旧结果。十连原动作与掉落时间轴不变。
- `ChopRefundFeedback.show(button,count,{audio})`在每刀已确认结果的原时间点同步调用，不等待飘字。按钮上方显示“斧技发动：返还X次”，最多4条，新条目向上推旧条目，1.6秒自动结束；显示层点击穿透、声音350ms限频。离页、隐藏、按钮移除及结果弹窗打开时清理，不累计到下一轮，也不再次发奖。
- `UI.playDropSound`按真实掉落品质选择：1/2原掉落，3珍品，4/5大奖；物理砍击仍用原音。奖励弹窗品质1/2使用统一短音，3/4/5使用独立出场音，技能触发使用用户提供的V2音源；发动前只结束本弹窗组上一声出现音的尾音，突出技能起音。声音分组限并发，跳过/关闭只停当前组，不影响BGM或锻造循环。
- `LoginBoot`在外部SDK前启动，初始shell为inert；公共资源下载校验、关键图片load+decode、装饰成功或用户简化、Auth事件绑定与公共小图解码共同放行。Auth绑定后`markRuntimeReady({imageAssets:getEntryDecodeImageAssets()})`，再用`LoginArt.init({prepared})`复用已解码图片，不重复下载墨纹。静态img使用`data-boot-src`，主图由解码阶段赋src，导航图完成后恢复，避免先行下载再强制下载；正式入口缺清单须报错并提供刷新重试。
- 首屏静态“入境准备”纸面、随机修行小笺和细进度条先显示，背景约50KB。长等待每6秒淡换一句，后台/出错/完成停止计时；减少动态模式无淡入淡出。所有下载校验占0–94%，登录与公共画面解码占后段，全部真实完成才100%，不能用计时器虚增。首屏4张主图失败重试；仅装饰解码失败可以简化，资源包失败不能跳过。
- `boot-assets.js`是生成的精确URL/字节/SHA256清单，包含公共图像、九把斧头90帧和13音频。准备页背景和进度素材优先，DPR只下载对应一套树图。`ResourcePack`最多6并发、校验完整字节、失败仅重试缺项。90帧和各大场景先下载，不同时全解码；登录后只解码当前修仙背景、当前树及其亮层、当前武器10帧。`AssetPreloader`共享解码和在途URL，取消只释放当前消费者。
- `resource-worker.js`仅为本项目`assets/runtime/`的图片/音频GET提供已备齐缓存，缓存名称固定`xianlai-resource-pack-v1`；不接管HTML、JS、配置、数据库或外部请求。实际URL含版本参数作为键，`x-xianlai-sha256`/`x-xianlai-bytes`校验清单变更；变更只补该项，保留两种密度已有缓存。音频完整缓存支持Range206/416。CacheStorage/SW不可用或超时则降级普通网络，不阻止登录。
- 每次改资源、音频映射或配置图标后，运行`node scripts/build_boot_assets.cjs`更新清单，再运行`node scripts/build_boot_assets.cjs --check`。不要手改清单或给所有旧资源统一换版本。实际浏览器检查`node tests/entry-preparation.browser-check.cjs`，无截图、无真实账号，覆盖首次接管、冷热缓存、音频范围与五视口。
- 外部字体CSS异步应用，SDK预取后在底部按顺序执行，避免二者挡住最初的加载画面。改资源只更新改变的URL；未改图片与声音继续使用原缓存。

## 15. 首次砍树引导

- 已确认方案见`FIRST_CHOP_GUIDE_PROPOSAL.md`，实现计划见`superpowers/plans/2026-09-10-first-chop-guide.md`。`first-chop-guide.js/css`独立负责遮罩、聚焦、气泡浮动和输入保护，不操作数据库或发奖励。
- `Auth.session`只保存已验证会话的角色、玩家数据角色及环境，不保存密码。登录数据、公共图片、当前武器动作全部就绪后设置；失败及登出清空。正式修炼者`totalChops===0`自动教学；已有累计次数、天道账号、测试账号均不强制自动弹出。
- `renderCultivate`在`MobileCultivation.mount`移动真实按钮完成后调用`startFirstChopGuide`。气泡采用现有已预载纸面，遮罩300ms后出现，2.6秒周期上下浮动5px，聚焦圈轻微呼吸；减少动态保持稳定，后台暂停。
- 聚焦孔跟随真实`#chop-btn`及溢出的斧头图，不复制按钮、不临时挪动底部布局。捕获输入只转交一次`onChop`；禁止其他点击、Escape、滚轮和触摸滚动。安卓返回键在引导活动期间也被拦截。
- 接受点击即淡去遮罩，清除十连状态并沿用`PlayerView.doChop()`及`OperationGuard`完成一刀；返回真实成功后结束。失败重新获取重绘后的真实按钮恢复引导；无次数或目标已失效则解除遮罩，不能把页面锁在禁用按钮上。旧会话/旧异步结果不得重新创建气泡。
- 以云端现有`total_chops`作为首次完成依据，不新增仅设备本地的完成开关。因此换设备/重装后保留已有进度；从未砍过但做过其他任务的正式账号会看到一次。离页、退出、登录失败清理所有引导监听、焦点属性、遮罩及滚动锁。
- 测试修炼者顶栏声音按钮旁显示小型“重播引导”入口。真实消耗一次次数，不清累计进度、不重置背包、不新增奖励；没有次数、其他操作忙碌或有弹窗时提示并拒绝开始。正式账号不出现该入口，不必为反复验收创建账号。
- 验证包括独立控制器状态测试、实际应用接入测试、安卓返回/后台保护，以及`tests/first-chop-guide.browser-check.cjs`的模拟账号多视口验证。核对真正砍树一次、失败重试、气泡动画在运行、输入保护及清理，不截屏或操作真实玩家数据。
