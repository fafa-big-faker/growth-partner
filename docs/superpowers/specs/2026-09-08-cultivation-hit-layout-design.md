# 仙树点击与砍树次数定位修复

用户已要求直接修复并按最佳方案推进，不重复确认。不更换资源、不改变已经验收的角色与仙树绘制顺序、不触碰玩家数据。

- 仙树失去点击源于 z-index:3 的角色透明矩形覆盖 z-index:1 的树。保留图层高低，在 `.cult-char` 和 `.char-img` 设置 `pointer-events:none`，点击穿过纯展示层。
- 次数角标目前与按钮并列，绝对定位参照整个 `.action-chop-area`，因此落在十连砍位置。把角标移进相对定位的砍树按钮，继续纯数字显示，固定在按钮底沿。
- 操作区纵向间距调整为20px，让底沿外露7px的次数角标与十连砍仍有13px间隔。角标不捕获指针，长数字不换行。
- 不提高树的图层，不用负边距硬拉次数位置。保持水墨资源、悬浮/下劈动效、十连节奏、三种树的大小与坐标不变。
- 验证桌面/手机、三种树的树冠/树干点击命中以及1/4/6位数的角标布局。只运行无账号的自动几何/命中检查，不做浏览器视觉验收。
