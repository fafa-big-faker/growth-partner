# V6 Ink Marks And Login Button

Approved by the user's request to import the two V6 atlases. The quality design supersedes the earlier jade-emblem proposal: use flat vertical pigment strokes, not jewels or glowing badges. No further approval gate is needed.

- Source: sibling `美术风格参考V6`, two 1536x1024 RGBA atlases. Preserve both originals; measure actual alpha, not RGB in transparent pixels. Cut quality marks from five 512x512 cells; cut login background and lettering from the two half-height cells. Remove only near-transparent noise, preserve real brush tips and calligraphy. Sixth quality cell is empty.
- Output: seven cropped PNGs under `assets/images/v6`, seven compressed WebPs under `assets/runtime/v6`: `quality/quality-1` through `quality-5`, `ui/login-brush`, `ui/login-lettering`. Record source fingerprints, measured cells/bounds, padding and runtime dimensions in manifests. Add a scoped V6-only build option without rewriting old artwork.
- Quality: replace the existing top-left thin CSS bar with the real marks, a stable 12x26px contain box with the visible stroke around 9x24px. Keep slots, quantity/new/lock badges and item icons unchanged. Both desktop inventory and body-mounted mobile drawer receive identical mapping 1 gray, 2 blue, 3 purple, 4 rose-red, 5 gold. No external glow; retain existing text quality information.
- Login: compose the real brush and white calligraphy as independent image layers within the existing native submit button. Visible text becomes the supplied four-character inscription. Hover/focus illuminates only lettering in about 180ms; press contracts slightly, no layout shift. Preserve loading, credentials, guards, delayed logo and reduced-motion behavior. Image failure retains a readable accessible text button.
- Preload exact versioned URLs (`xianlai-v6-20260908`); gameplay waits for the five small quality marks, but login button appearance never gates authentication. Update relevant script/style versions only.
- Validation: source alpha/crop/size/byte checks, all Node tests, existing image tests, JS/Python syntax, mocked browser geometry and interaction across desktop/mobile without screenshots or real accounts. Do not modify configuration, Supabase, player data or credentials.
- Release the tested scoped commit to origin/main; confirm Pages success and deployed bytes. Leave user-owned untracked supabase/ untouched.
