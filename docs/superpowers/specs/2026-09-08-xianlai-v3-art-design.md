# 仙来 V3 美术接入

用户已按先前提示词提供四张原图并要求直接替换、加动效。本次不再生图，不重复征求批准，不改游戏配置或用户数据。

- 从仓库同级 `美术风格参考V３` 导入登录背景、独立仙来logo、2x3框图集和3x2功能图标图集。源图均保留，切片和透明清噪由确定性脚本执行。
- 14张WebP放入 `assets/runtime/v3`，约443KB；长框保持原生宽度，单独的尺寸规则避免旧版256px上限造成模糊。源和运行清单记录尺寸、来源及九宫格slice。
- 顶部工具条、仙阶状态条、背包、装备栏和底部导航采用独立九宫格背景层。旧对应底纹移除，保持现有区域、格子、角色和树坐标不变。背景层点击穿透，延续已修复的仙树热区和砍树次数定位。
- 功能图标经统一映射切换修仙、任务、商店、邮件、成就及声音；静音以同一喇叭图加斜线展示，不继续由音频管理器写入emoji。
- 登录页采用真实透明logo、清新山水底图、新纸面按钮。logo在加载后进行一次1.5秒墨迹揭示，周边低透明细墨纹缓慢变化，按钮按下短墨波；不为了动画延迟登录。
- 动画模块只消费实际加载百分比并平滑显示。界面隐藏、文档后台、离屏时暂停；减少动态效果设置禁用装饰动画。保持真实登录form与浏览器密码管理行为。
- 首屏清单和预加载改用新图；新样式与脚本带版本。保留未替换的树、角色、砍树按钮、锻造入口和道具精修图。
- 按用户约定不做浏览器视觉验收；可检查图片接触图与无账号的自动几何、资源加载、交互和动画生命周期。
