# 珍稀奖励墨破现宝

用户已确认“墨破现宝”的帧动画方向并交付两张图集与两条加强音效；直接实现、验证、发布，不重复确认。

- 原图在`../美术风格参考V7/掉落特效-珍品.png`及`掉落特效-神仙品.png`。实际均1448×1086 RGBA，4列3行，每格362方形，共12帧。整格切割、共享固定中心与统一缩放，不逐帧按轮廓居中。源文件永久保留。
- 导出24张原格PNG归档及两张1024×768透明WebP运行图集，单格256，原格统一缩成224并补16透明边。只清alpha<5彩噪，保留所有其他墨迹与光屑。两套共用所有道具，不新增按道具复制的特效。
- 现有品质墨团保留为最终背景，出场替换为真实图集逐帧爆开。只在当前格局部放大到160%（珍品）/180%（神仙品），不改变格子及弹窗最大尺寸；道具始终在特效上方。包含特效的弹窗固定预留26px顶部绘制空间，左右滚动绘制区向纸面内扩展26px并以同等padding维持列宽，演出期间不推挤布局。神品/仙品共用高档图集，通过局部色调分别偏玫紫/暖金。
- 珍品新音频880ms，峰约394ms；高档1280ms，峰约429ms。图标分别240/260ms开始回弹，峰约400/430ms；墨花第6帧峰分别380/410ms，与声音同步。原名与基础数量从图标出现时可见，完整出场结束后再停留300ms。
- 珍品帧起点(ms)：0/60/120/210/290/380/450/520/610/700/770/830，880ms结束。高档：0/80/170/260/340/410/510/640/790/960/1110/1220，1280ms结束。逐帧运动通过离散图片位移实现，避免连续滑动串格。
- 珍品回弹320ms，高档340ms；不添加全屏闪白、规则发光圆环、额外粒子、旋转整张墨团或全屏震动。图集结束归于现有品质墨团。
- 类型1技能仍逐次1300ms，前置原名停留300ms；类型2仍在砍树按钮飘字。奖励结果只读，不修改计算和到账逻辑。
- 两帧绘制起点、单个后续阶段定时器、跳过/关闭/隐藏清理继续保留。减少动态隐藏爆发层并取消回弹，信息按原次序呈现。图片已纳入首屏精确清单和解码队列，不能在出场时临时下载。
- 自动滚入视野时包括真实墨迹顶部：256px帧中珍品最小y=29、高档y=23，不能只按道具矩形滚动。边界测试需覆盖所有12帧实际alpha范围与弹窗/滚动区两层裁切，不只验证图集单格。
- 原11个其他声音完全不变；加强版只替换原两条出场音，保留完整长度和动态，更新各自缓存URL。只同步此次资源清单，不修改飞书或数据库。
- 测试包括确定性裁切/重建与透明边、实际图集解码和12帧位置、重音时间、独立品质色、五视口布局、跳过/取消、减少动态和电脑真实入口；无截图、无真实账号写入。
