# V5 Ink And Paper Implementation Plan

> **For agentic workers:** Use executing-plans with bounded parallel ownership. Unavailable subagent-driven-development/worktree/finishing skills are replaced by the existing project workflow. The user has approved this design and requested execution in the shared project; do not repeat approval questions.

**Goal:** Replace placeholder visuals with approved V5 artwork and unify readable task, shop and mobile inventory surfaces.

**Architecture:** Extend the existing deterministic image importer/builder, LoginArt lifecycle and scoped page styles. Main owns mobile CSS and integration; separate workers own V5 assets, login animation and task/shop CSS. Do not change gameplay behavior.

**Tech Stack:** Vanilla JS, CSS nine-slice, Canvas bitmap drawing, Pillow/WebP, Node tests and local mocked Playwright checks without screenshots.

## Global Constraints

- Use only the supplied two source PNGs; no paid generation or source overwrites.
- No database/config/password-flow changes, no edits to user-owned `supabase/`.
- Preserve current scene and chop timing, operation locks, responsive drawer behavior, task filter and scroll state.
- Fail optional art gracefully; clean timers/frames/listeners; obey reduced motion and hidden state.
- A single step with no progress for ten minutes requires diagnosis and reporting, not silent retries.

## Task 1: Deterministic Assets (Asset Worker)

Files: create `scripts/import_xianlai_v5_art.py`, `tests/xianlai-v5-assets.test.py`; update `scripts/build_runtime_images.py`; generate only `assets/images/v5/**` and `assets/runtime/v5/**`.

Interface: runtime `ui/task-paper.webp`, `ui/shop-paper.webp`, `effects/ink-01.webp` to `ink-06.webp`; same manifest groups/spec fields as V4 (`path`, `size`, `slice`). Tell main and page worker the measured runtime slices immediately.

- [x] Test two paper plus six ink outputs, source hashes, alpha extrema, safe bounds, valid nine-slice sums, clean invisible borders, retained translucent ink and total runtime <=1MiB.
- [x] Import measured alpha frame bounds with padding; convert ink white-luminance into alpha, never threshold legitimate dark ink away. Validate source dimensions and preserve originals.
- [x] Extend runtime builder with `build_v5_assets()` and a `--v5-only` option; paper limit 960px wide, ink 512px square. Generate manifests and run the new asset tests plus existing asset tests.

```python
assert len(manifest['ui']) == 2
assert len(manifest['effects']) == 6
assert all(sum(spec['slice'][::2]) < spec['size'][1] for spec in manifest['ui'].values())
```

## Task 2: Real Ink Motion (Login Worker)

Files: `login-art.js`, optionally `login-art.css`, `tests/login-art.test.js`. Do not edit index/app/shared browser checker.

Interface: retain `LoginArt.create/init/setLoading/setVisible` and instance `destroy`; expose `LoginArt.getImageAssets()` for exact versioned decoration URLs with `v=xianlai-v5-20260908` so main can preload consistently.

- [x] Replace water-oriented assertions with texture-load/draw, movement, form avoidance, failed texture handling, reduced motion and lifecycle assertions; retain loading progress and Logo tests.
- [x] Load the six runtime textures once per instance without awaiting authentication; implement art-led traces/fragments, curved motion, controlled local deformation, and bounded drawing. Remove the old lake/water code entirely.
- [x] Ensure destroyed or hidden instances cannot be restarted by late image callbacks; failed decoration must not run an endless empty animation loop. Retain progress-only animation if needed.
- [x] Run `node --test tests/login-art.test.js` and report expected Canvas mock changes to main.

```js
assert.ok(LoginArt.getImageAssets().every(url => url.includes('assets/runtime/v5/effects/ink-')));
assert.equal(LoginArt.getImageAssets().length, 6);
```

## Task 3: Paper Pages (Page Worker)

Files: `ink-pages.css`, new `tests/v5-paper-pages.test.js`. Do not edit shared app/index/browser checks.

- [x] Assert scoped task/shop use new bitmap border-image, body text >=14px, neutral hierarchy and no new page blur/card nesting.
- [x] Use two measured paper frames with content padding clear of corners, paper fallback for failed images, responsive tall copy and fixed readable button areas.
- [x] Apply neutral hierarchy to all task/account/sign-in/shop/withdrawal texts, retaining semantic state and quality colors. Audit existing high-specificity legacy rules.
- [x] Run focused tests and report any markup change strictly needed to main.

```js
assert.match(css, /assets\/runtime\/v5\/ui\/task-paper\.webp/);
assert.match(css, /assets\/runtime\/v5\/ui\/shop-paper\.webp/);
assert.match(css, /#252b29/);
```

## Task 4: Mobile And Integration (Main)

Files: `mobile-cultivation.css`, `app.js` preload only, `index.html` versions, `tests/mobile-cultivation.browser-check.cjs`, `tests/ink-polish.browser-check.cjs`, version assertions and new scoped integration tests; maintenance docs.

- [x] Reuse V3 topbar on the fixed summary trigger, clear drawer legacy background/blur, retain external V4 paper and grid art, style neutral headers/labels and readable tabs without nesting decorative frames.
- [x] Add two paper URLs to initial assets, update changed dependency versions to `xianlai-v5-20260908`; preserve resource transaction logic. Review correction: optional `LoginArt.getImageAssets()` textures load only in LoginArt, never in the awaited game preload queue.
- [x] Replace water pixel assertions with decoded-texture movement and visible coverage/exclusion checks; require paper images loaded, adequate neutral text contrast, long content fit and working task/shop controls.
- [x] Extend mobile tests to assert background asset reuse, no inner blur/old backing, drawer preservation and no overflow. Execute existing four/five viewport checks without screenshots.
- [x] Run full Node suite, all image validations, JS/Python syntax, source fingerprint and scoped diff checks; update playbook and asset pipeline with exact commands/sizes.

Verification: 241 Node tests, 24 image tests, four login/page and five mobile/scene viewports passed. Both original fingerprints are unchanged; eight runtime assets total 185,918 bytes. Browser checks are mocked with no screenshots or account operations. Final staged credential scan is part of the release gate below.

Release gate: commit only scoped files, push main, wait for Pages success and compare deployed resources to committed bytes. Record actual deployment confirmation in the release response.
